import React from 'react';
import styles from './Pagination.module.scss';

const Pagination = () => {
  return <div className={styles.wraper}>Pagination component</div>;
};

export default Pagination;
